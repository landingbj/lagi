mcps:
  # set default driver
  servers:
    - name: baidu_search_mcp
      url: http://appbuilder.baidu.com/v2/ai_search/mcp/sse?api_key=Bearer+your-key
      # The driver is optional, if not specified, the default driver will be used.
      # driver: ai.mcps.CommonSseMcpClient