system_title: Lagi

LLM:
  embedding:
    type: Random
  backends:
    - name: vicuna-test1
      type: Vicuna
      enable: true
      priority: 100
      model: /mnt/data/vicuna-13b-v1.5-16k
      api_address: http://116.255.226.214:8090/v1/chat/completions

vector_store:
  type: Chroma
  default_category: demoll5
  url: http://localhost:8000

ASR:
  backends:
    - name: asr-test1
      type: Landing
      enable: true
      priority: 10

TTS:
  backends:
    - name: tts-test1
      type: Landing
      enable: true
      priority: 10

image_generation:
  backends:
    - name: image-generation-test1
      type: Landing
      enable: true
      priority: 10

image_captioning:
  backends:
    - name: image-captioning-test1
      type: Landing
      enable: true
      priority: 10

image_enhance:
  backends:
    - name: image-enhance-test1
      type: Landing
      enable: true
      priority: 10

video_generation:
  backends:
    - name: video-generation-test1
      type: Landing
      enable: true
      priority: 10

video_track:
  backends:
    - name: video-track-test1
      type: Landing
      enable: true
      priority: 10

video_enhance:
  backends:
    - name: video-enhance-test1
      type: Landing
      enable: true
      priority: 10
